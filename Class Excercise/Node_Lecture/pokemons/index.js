const pokemon = require('pokemon');

console.log(poke);

let pokeArray = [];
for(let i = 0; i < 5; i++);{
    // let poke = pokemon.randon();
    pokeArray.push(pokeArray.randon());
}

console.log(pokeArray);